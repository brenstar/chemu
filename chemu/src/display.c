
#include <stdio.h>


#define CHIP_DISPLAY_ADT
typedef uint8_t[]

#include "display.h"

/*
static void clear(FILE* stream);
static void put(FILE* stream, char c);
static void set_cur_pos(FILE* stream, int rCursor, int cCursor);

//
// Name:    clear
//
static void clear(FILE* stream) {
    fprintf(stream, "\033[2J");
    fflush( stream );
}

//
// Name:    put
//
static void put(FILE* stream, char character ) {
    fputc(character, stream);
    fflush(stream);
}

//
// Name:    set_cur_postition
//
static void set_cur_pos(FILE* stream, int rCursor, int cCursor) {
    fprintf(stream, "\033[%d;%dH", rCursor, cCursor );
} // set_cur_pos
*/

